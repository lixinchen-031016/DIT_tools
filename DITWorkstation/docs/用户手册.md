# DIT 工作站 — 专业摄影数据管理 用户手册

## 应用总览

**应用名称**：DIT 工作站 — 专业摄影数据管理

**版本格式**：`alpha.YYYYMMDD`。版本号显示在窗口标题、底部状态栏和「帮助 → 关于 DIT 工作站」中；日期部分为启动/打包当天的本地日期。

DIT 工作站是一款面向专业摄影工作流的桌面应用，覆盖从媒体导入、数据备份、RAW 提取、文件重命名、拍摄日志、素材信息、素材检索到报告生成的完整数据管理链路，帮助 DIT（数字影像工程师）在片场高效、安全地管理海量摄影素材。

### 运行环境与平台说明

- **macOS / Windows**：支持源码运行和对应平台的 PyInstaller 打包脚本。
- **Linux**：支持从源码运行；当前没有 Linux 原生打包脚本，也没有 Windows/Linux 原生 GUI 验证结果。
- **视频元数据**：macOS/Windows 正式构建要求 MediaInfo，并随产物分发动态库；运行时无需用户额外安装。
- **视频缩略图**：依次尝试 `ffmpeg`、macOS QuickLook 和随包提供的 PyAV；全部解码失败时显示占位图，不影响素材入库。
- **PDF 报告**：优先使用系统中文字体；缺失时回退至 ReportLab 内置 CJK CID 字体，仍可生成报告。

> **当前实现边界**：Linux 支持源码运行，但目前没有 Linux 原生打包脚本；Qt `offscreen` 测试不能替代 Windows/macOS 原生 GUI、文件系统和打包验证。视频缩略图依次尝试 `ffmpeg`、macOS QuickLook 和 PyAV；MediaInfo 是视频元数据读取的可选/构建依赖。

### 当前实现边界与数据安全

- 删除项目或素材只删除 SQLite 中的元数据（或移入回收站），不会删除源媒体文件。
- 「清理丢失素材」只删除数据库中的失效记录，不操作磁盘文件。
- 取消导出、报告或归档后，已生成的部分输出不应作为完整交付物使用。
- 原生 GUI、文件系统权限、外接存储卡和 PyInstaller 产物仍需在目标操作系统上单独验收。

### 主窗口布局

- **顶部菜单栏**：含「设置」菜单（快捷键 `Ctrl+,`，macOS 为 `Cmd+,`），可打开设置对话框、回收站和「任务中心…」。
- **左侧导航栏**：宽度 200px，深色主题，列示 9 个功能模块入口。
- **右侧内容区**：展示当前选中模块的操作界面。
- **底部状态栏**：显示当前任务状态与存储卡检测提示。
- **窗口尺寸**：最小 1200×800，默认 1400×900。

### 导航栏功能模块

| 序号 | 图标 | 模块名称 | 说明 |
|------|------|----------|------|
| 1 | 🏠 | 项目概览 | 当前项目进度看板与 SOP 操作链引导 |
| 2 | 📁 | 媒体导入 | 扫描并导入源文件夹中的图片/视频/RAW 文件 |
| 3 | 📦 | 数据备份 | 多目标并行备份源数据，支持校验与 MHL 清单 |
| 4 | 🎞 | RAW 提取 | 依据筛选后 JPG 反查并提取对应 RAW 文件 |
| 5 | ✏️ | 文件重命名 | 按命名规则模板批量重命名文件 |
| 6 | 📋 | 拍摄日志 | 记录场景/镜头/镜次信息并关联素材 |
| 7 | 🔍 | 素材检索 | 按项目/关键词/场景/镜头/日志/评级等多条件检索素材 |
| 8 | ℹ️ | 素材信息 | 查看素材 EXIF 与元数据详情，设置镜次评级，检测和清理丢失文件记录 |
| 9 | 📊 | 报告生成 | 生成数据备份报告或素材统计报告（PDF） |

### 使用场景：团队模式与个人模式

在「设置 → 使用场景」中可以切换应用模式，切换后重启应用生效：

- **团队模式（默认）**：显示完整的 9 个功能模块，适合包含工作区、拍摄日志、评级、报告和多目标备份的完整 DIT 流程。
- **个人模式**：保留项目概览、媒体导入、数据备份、RAW 提取、文件重命名、素材检索和素材信息 7 个模块；隐藏工作区操作、拍摄日志、评级、报告、模板、归档/恢复和相机卡自动化入口，备份限制为单目标。
- 模式切换只影响界面和交互入口，不删除数据库数据；切回团队模式后，原有数据仍可读取。
- 个人模式下新项目自动归入默认工作区，不显示工作区选择操作。

### 数据与文件位置

| 项目 | 开发模式路径 | 打包后路径（macOS） | 打包后路径（Windows） | 打包后路径（Linux） |
|------|-------------|---------------------|----------------------|---------------------|
| 数据库文件 | `data/dit_workstation.db` | `~/Library/Application Support/DITWorkstation/` | `%APPDATA%\DITWorkstation\` | `~/.local/share/DITWorkstation/` |
| 报告输出目录 | `~/Documents/DIT_Reports/` | 同左 | 同左 | 同左 |
| 日志文件目录 | `~/.dit_workstation/logs/` | 同左 | 同左 | 同左 |

> 若数据库、设置或报告目录不可写（如 macOS TCC 权限拒绝），应用会尝试回退到 `~/.ditworkstation/`，最后回退到系统临时目录。
> 日志目录初始化失败时应用仍可运行并输出控制台日志；通过设置重新指定可写日志目录后，文件日志可以恢复。

### 标准 SOP 操作链

DIT 工作站围绕以下标准操作链设计：

```
⓪ 创建工作区 → ① 在工作区内创建项目 → ② 媒体导入 → ③ 数据备份 → ④ 拍摄日志补录 → ⑤ 素材检索/信息/报告
```

- **工作区**对应一个物理目录（如「2026 春季广告片」对应 `/Volumes/Work/2026SpringAd`）
- **项目**是单次拍摄任务的素材集合（如「镜头组A」），归属于某个工作区
- 一个工作区下可创建多个项目

---

## 首次启动：首启向导

首次启动应用且数据库中无任何项目时，会自动弹出首启向导，引导完成项目初始化。

### 向导流程

团队模式显示 5 页，个人模式显示 3 页；两种模式都会创建项目，个人模式自动使用默认工作区。

1. **欢迎页**：介绍 SOP 操作链与工作区/项目概念
2. **创建工作区（团队模式）**：输入工作区名称（必填）、物理目录（可选，浏览选择）、描述（可选）
3. **创建项目**：输入项目名称（必填）与描述（可选）
4. **SOP 操作链提示（团队模式）**：媒体导入、数据备份、拍摄日志补录的详细操作说明
5. **完成页（团队模式）**：后续工作流建议，点击「完成」自动跳转到「媒体导入」视图

个人模式跳过「创建工作区」和「SOP 操作链提示」页，直接从欢迎页进入创建项目，再进入完成页。

### 注意事项

- 向导为非阻塞对话框，可随时取消；取消后仍可正常使用主程序
- 完成向导后，创建的工作区和项目会自动设为当前全局工作区/项目
- 后续如需新建工作区或项目，可在各功能模块的左侧面板操作

---

## 工作区与项目管理

### 工作区-项目两级结构

DIT 工作站采用「工作区 → 项目」两级结构组织素材：

- **工作区**：项目的父级容器，对应一个物理目录，用于组织一组相关项目
- **项目**：单次拍摄任务的素材集合，归属于某个工作区
- 严格 1:N 关系：一个工作区下可创建多个项目

### 工作区操作

各功能模块左侧面板顶部均有工作区下拉框与操作按钮：

- **选择工作区**：从下拉框切换工作区，项目列表自动过滤为该工作区下的项目
- **新建工作区**：点击「+ 新建工作区」按钮，弹出对话框填写名称、物理目录、描述
- **编辑工作区**：点击「编辑」按钮修改当前工作区信息
- **删除工作区**：
  - 默认工作区（id='default'）不可删除
  - 删除非默认工作区时，其下所有项目自动归入默认工作区
  - 删除工作区不会删除项目内的实际文件

### 项目操作

选择工作区后，项目列表显示该工作区下的所有项目：

- **新建项目**：点击「+ 新建项目」按钮，输入项目名称与描述（必须先选择工作区）
- **删除项目**：选中项目后点击「删除」按钮（事务级联清理素材与日志记录，不删除实际文件）
- **切换项目**：点击项目列表中的项目，全局当前项目会同步切换，其他视图自动刷新

---

## 模块 1: 项目概览

### 界面布局

- **顶部**：工作区/项目选择器
- **中部**：4 张统计卡片，展示当前项目的进度数据
- **项目管理区**：🗜 归档当前项目… / ↩ 恢复项目… / 🧩 从模板新建项目… / 💾 保存当前项目为模板…
- **底部**：SOP 操作链引导区，提供「下一步」提示与 4 个快捷跳转按钮；下方展示「🕘 最近操作」审计记录（最近 5 条）

### 统计卡片

| 卡片 | 显示内容 |
|------|----------|
| 素材总数 | 已导入的素材数量与总大小 |
| 备份覆盖 | 已备份素材占比 |
| 日志关联 | 已关联拍摄日志的素材数量 |
| 评级覆盖 | 已设置评级的素材数量 |

### SOP 快捷跳转

4 个按钮根据当前进度提供跳转：

- **① 媒体导入**：跳转到媒体导入视图
- **② 数据备份**：跳转到数据备份视图
- **③ 拍摄日志**：跳转到拍摄日志视图
- **④ 生成报告**：跳转到报告生成视图

### 动态提示

底部提示文案会根据项目进度动态生成，例如：
- 无素材时：「建议从媒体导入开始」
- 有素材未备份时：「建议尽快进行数据备份」
- 有素材未关联日志时：「建议补录拍摄日志」

### 项目管理

#### 归档当前项目

- 点击「🗜 归档当前项目…」把项目信息 + 拍摄日志 + 素材元数据打包为 zip（可选附带素材文件副本）。
- 归档包内含 `manifest.json` / `assets.json` / `logs.json` / `checksums.txt`，自包含、可交接。
- 不附带素材文件时体积小，适合日常备份元数据；附带素材文件时适合长期保存与项目交接。

#### 恢复项目

- 点击「↩ 恢复项目…」选择归档 zip，从归档包重建项目到当前工作区。
- 可选还原素材文件：还原到临时目录并自动校验校验和，全部通过后才移入目标位置，失败不会留下半成品。
- 与现有项目重名时自动追加时间戳后缀，不会覆盖。
- 恢复过程内置安全防护：校验 zip 成员路径（拒绝绝对路径 / 父目录跳转）、限制解压大小并阻止符号链接越界。

#### 项目模板

- 「🧩 从模板新建项目…」：选择模板 → 填写名称与工作区 → 一键建项（模板中的描述 / 工作目录自动预填）。
- 「💾 保存当前项目为模板…」：把当前项目的名称 / 描述 / 工作目录保存为模板，供后续复用。
- 首次初始化自动预置「标准影视项目」模板；模板通过「项目概览」的模板按钮创建与复用。

#### 最近操作

- 页面底部展示最近 5 条操作审计记录（导入、备份、RAW 提取、重命名、评级、标签更新等自动留痕）。

---

## 模块 2: 媒体导入

### 界面布局

界面采用左右分栏结构：

- **左侧**：工作区下拉框 + 项目列表 + 「+ 新建工作区」/「+ 新建项目」按钮。
- **右侧**：
  - **导入源**：文件夹选择输入框 + 「浏览...」按钮。
  - **扫描选项**：复选框组（递归扫描 / 包含图片 / 包含视频 / 包含 RAW）+ 「扫描」按钮。
  - **待导入文件表格**：6 列（导入 / 文件名 / 类型 / 大小 / 修改时间 / 路径），支持「☑ 全选 / 全不选 / ⇄ 反选」；选中行在右侧显示缩略图预览。
  - **按时间筛选**：勾选后只导入指定时间范围内的文件（按文件修改时间，存储卡上通常等于拍摄时间），提供「今天 / 最近7天 / 清除范围」快捷按钮。
  - **导入选项**：计算校验和复选框、复制到工作区复选框（自动填充工作区目录）、关联拍摄日志下拉框。
  - **操作按钮**：导入按钮 + 取消按钮。
  - **进度与状态**：进度条、状态标签、导入日志。

### 操作流程

1. 在左侧选择工作区，再选择项目（或点「+ 新建项目」创建新项目）。
2. 点击「浏览...」选择包含媒体文件的文件夹。
3. 勾选要扫描的文件类型（图片 / 视频 / RAW），可勾选「递归扫描」包含子文件夹；如需只导入某一天的素材，勾选「按时间筛选」并设置时间范围。
4. 点击「扫描」按钮，待导入文件表格显示扫描结果。
5. 在导入选项中：
   - 勾选「计算校验和」为每个文件生成唯一哈希值用于查重；
   - 如需将文件复制到工作区，勾选「复制到工作区」（目录自动填充为当前工作区路径，无需重新选择）；
   - 如需关联拍摄日志，从下拉框选择已创建的日志（会自动带出 scene / shot 信息）。
6. 点击「导入」按钮开始导入，进度条显示进度，可随时点「取消」中断。
7. 导入完成弹出结果：成功 N 个、跳过 N 个（重复）、失败 N 个。

### 两种导入模式

- **引用模式（默认）**：原文件位置保持不动，仅记录路径到数据库。
- **复制模式（勾选「复制到工作区」）**：将文件复制到当前工作区对应的物理目录，重名文件自动加 `_N` 后缀。

### 查重逻辑

按项目内文件路径查重，已导入的文件会被跳过。

### 注意事项

- 导入前请确保存储卡 / 源文件夹可正常读取。
- 校验和计算会增加导入时间，但便于后续完整性验证。
- 关联拍摄日志后，素材的 scene / shot 字段会自动填充日志对应值。
- 复制模式下目录自动取自当前工作区路径，无需手动选择。
- 缩略图预览基于本地缓存，首次生成稍慢，之后即时显示。

---

## 模块 3: 数据备份

### 界面布局

- **顶部**：工作区/项目选择器（可选「不关联项目，仅做文件备份」）
- **源路径选择**：输入框 + 「浏览...」按钮。
- **备份方案模板**：方案下拉框 + 「保存当前方案」/「删除方案」按钮（可复用目标列表、校验算法与验证选项）。
- **备份目标列表**：目标路径列表 + 「+ 添加目标」/「移除」按钮；每个目标实时显示磁盘剩余空间（备份前自动预检）。
- **配置区**：校验算法下拉框（XXHash64 / MD5）+ 「拷贝后验证」复选框。
- **操作按钮**：开始备份 / 取消 / 📄 导出 MHL 校验清单 / 🔍 校验已有备份。
- **进度与状态**：进度条（0-1000 精度）、状态标签、操作日志。
- **备份历史**：最近 20 条备份作业记录（时间 / 项目 / 源路径 / 目标数 / 状态 / 失败文件），支持断点续传与失败文件重试。

### 操作流程

1. （可选）在顶部选择要关联的项目；若仅做文件备份可不关联项目。
2. 点击源路径旁的「浏览...」选择存储卡或源文件夹，系统自动扫描显示文件数和总大小。
3. 点击「+ 添加目标」逐个添加备份目标路径（支持多目标并行备份，最多 4 个并行）。
4. 选择校验算法（推荐 XXHash64，速度更快）和是否「拷贝后验证」。
5. （可选）点击「保存当前方案」把本次配置保存为备份方案，下次从下拉框直接选择复用。
6. 点击「开始备份」，系统先对每个目标做磁盘空间预检（空间不足会拒绝启动），再并行拷贝到所有目标。
7. 每个文件拷贝完成后自动验证（如开启），进度条实时更新。
8. 完成后弹出窗口显示每个目标的成功 / 失败状态。
9. 若关联了项目，备份完成后会自动回写素材的 `backup_locations` 字段。

### 断点续传与失败重试

- 备份中断（拔卡 / 断电 / 取消）后，未完成文件会记入备份历史。
- 重新备份时，目标中已存在且校验一致的文件自动跳过，只补拷缺失 / 失败文件。
- 在「备份历史」选中某条作业，可查看失败文件列表；点击「↻ 重试失败文件」只重试失败项，其余文件自动跳过。
- 备份任务运行中会实时保存快照，应用异常退出后重启可恢复到上次进度。

### 校验已有备份

- 点击「🔍 校验已有备份」：按素材记录的备份位置对备份盘做独立完整性校验（存在性 + 校验和比对），可检测位错误 / 文件丢失。
- 仅当备份时关联了项目，素材的备份位置才会被记录，因此该项目素材才能被校验。

### 导出 MHL 校验清单

- 完成一次备份后，点击「📄 导出 MHL 校验清单」可把本次备份的源文件哈希列表导出为 ASC MHL（Media Hash List）2.0 格式 XML 文件，用于交接与第三方核验。

### 技术细节

- 多目标使用线程池并行拷贝（最多 4 个并行）。
- 拷贝缓冲区 8MB，校验和缓冲区 4MB。
- 拷贝后验证：对目标文件重新计算校验和并与源文件比对，不一致则报错。
- 备份前磁盘空间预检：任一目标剩余空间不足则整体取消并提示。

### 注意事项

- 备份过程中不要拔出存储卡。
- 建议始终开启「拷贝后验证」确保数据完整性。
- 多目标备份时，某个目标失败不影响其他目标。
- 关联项目后，备份位置会记录到素材信息中，便于后续追溯。

---

## 模块 4: RAW 提取

### 界面布局

- **顶部**：工作区/项目选择器（可选「不关联项目，仅提取文件」）
- **路径配置组**：JPG 文件夹、RAW 源文件夹、输出文件夹，各带「浏览...」按钮。
- **选项**：「提取后验证」复选框。
- **操作按钮**：扫描匹配 / 开始提取 / 取消按钮。
- **匹配结果表格**：3 列（JPG 文件 / 匹配 RAW / 状态）。
- **进度与状态**：进度条、状态标签。

### 操作流程

1. （可选）在顶部选择要关联的项目；若仅提取文件可不关联项目。
2. 设置「JPG 文件夹」（筛选后的 JPG 所在目录）。
3. 设置「RAW 源文件夹」（原始 RAW 文件所在目录）。
4. 设置「输出文件夹」（提取后的 RAW 存放目录）。
5. 可勾选「提取后验证」。
6. 点击「扫描匹配」查看 JPG 与 RAW 的匹配结果（状态显示「已匹配」或「未找到」）。
7. 确认匹配结果后点击「开始提取」，系统将匹配的 RAW 文件复制到输出目录。
8. 完成后显示提取数量、未找到数量、失败数量。
9. 若关联了项目，提取出的 RAW 文件会自动入库到该项目，并继承原 JPG 的日志关联（log_id）。

### 匹配逻辑

按文件名 stem（不含扩展名）小写比对，例如 `IMG_001.jpg` 匹配 `IMG_001.cr2`。

### 注意事项

- 确保 JPG 和 RAW 文件名前缀一致。
- 重名文件自动加 `_N` 后缀。
- 关联项目后，提取的 RAW 会自动入库并继承 JPG 的日志关联。

---

## 模块 5: 文件重命名

### 界面布局

- **文件夹选择**：输入框 + 「浏览...」按钮。
- **命名规则组**：可编辑下拉模板（4 种预设 + 自定义）。
- **参数输入框**：场景号 / 镜头号 / 镜次号、前缀 / 起始序号 / 序号位数。
- **操作按钮**：预览 / 执行重命名按钮。
- **预览结果表格**：2 列（原文件名 / 新文件名）。

### 操作流程

1. 点击「浏览...」选择要重命名的文件夹，系统自动扫描文件。
2. 在命名规则下拉框选择或自定义模板。
3. 填写场景号、镜头号、镜次（如 `S001` / `001A` / `T01`）。
4. 设置前缀、起始序号、序号位数（如 `001`）。
5. 点击「预览」查看重命名结果。
6. 确认无误后点击「执行重命名」（不可撤销）。
7. 重命名完成后，数据库中素材的文件路径会自动同步更新。

### 模板变量

| 变量 | 含义 |
|------|------|
| `{scene}` | 场景号 |
| `{shot}` | 镜头号 |
| `{take}` | 镜次号 |
| `{original}` | 原文件名（不含扩展名） |
| `{number}` | 序号（从起始序号开始，按序号位数补零） |
| `{prefix}` | 自定义前缀 |
| `{suffix}` | 自定义后缀 |
| `{date}` | 当前日期（YYYYMMDD） |

### 预设模板

1. `{scene}_{shot}_{take}_{number}`
2. `{scene}_{shot}_{take}_{original}`
3. `{prefix}_{scene}_{number}`
4. `{date}_{scene}_{shot}_{number}`

### 注意事项

- 执行重命名不可撤销，请先预览确认。
- 重名文件自动加 `_N` 后缀防止覆盖。
- 文件扩展名保持不变。
- 重命名后数据库中的素材路径会自动同步，无需重新导入。

---

## 模块 6: 拍摄日志

### 界面布局

左侧为工作区下拉框 + 项目列表 + 「新建项目」/「删除」按钮。右侧上下分栏：

- **上部**：
  - 拍摄日志表单（场景 / 镜头 / 镜次、摄影机 / 镜头、ISO / 光圈 / 快门、描述、备注、「选择关联素材」按钮 + 已选数量标签、「从代表素材填充 EXIF」按钮、「添加日志」按钮）。
  - 日志列表表格（场景 / 镜头 / 镜次 / 描述 / 摄影机 / 时间 6 列）。
  - 「删除日志」按钮。
- **下部**：双标签页
  - **标签页「关联素材」**：关联素材 / 解除关联按钮 + 素材表格（文件名 / 类型 / 大小 / 场景 / 镜头 5 列）。
  - **标签页「项目素材」**：为选中素材创建日志按钮 + 素材表格（文件名 / 类型 / 大小 / 场景 / 镜头 / 关联状态 6 列，支持多选）。

### 操作流程 — 方式一（先填表单再选素材）

1. 在左侧选择工作区，再选择项目。
2. 在表单填写场景 / 镜头 / 镜次等信息。
3. 点击「选择关联素材」弹出对话框，可搜索过滤，勾选要关联的素材，确认。
4. 标签显示「已选 N 个素材」。
5. 点击「添加日志」，系统创建日志并关联选中素材，同时把 scene / shot 同步写入素材。
6. 自动选中新日志，底部「关联素材」标签页显示刚关联的素材。
7. 弹出成功提示。

### 操作流程 — 方式二（先选素材再填表单）

1. 在左侧选择工作区，再选择项目。
2. 切换到底部「项目素材」标签页，显示项目所有已导入素材及关联状态。
3. 多选要记录日志的素材（Ctrl / Cmd + 点击多选）。
4. 点击「为选中素材创建日志」，系统将选中素材加入待关联列表。
5. 在上方表单填写场景 / 镜头 / 镜次等信息。
6. 点击「添加日志」，系统创建日志并关联素材。

### 从代表素材填充 EXIF

点击「从代表素材填充 EXIF」按钮，系统会从当前选中（或第一个）素材的元数据中自动带出：
- 摄影机型号（camera_make / camera_model）
- 镜头型号（lens_model）
- ISO、光圈（aperture）、快门速度（shutter_speed）
- 焦距（focal_length）

### 关联素材管理

- 选中某条日志后，底部「关联素材」标签页显示该日志关联的所有素材。
- 点「+ 关联素材」可为当前日志追加关联更多素材（弹出选择对话框）。
- 选中素材后点「解除关联」可取消关联（素材不被删除，仅清除 `log_id`）。
- 删除日志时，关联素材的 `log_id` 会被级联清除（素材不被删除）。

### 注意事项

- 创建日志时同步 scene / shot 到素材，解除关联时不会恢复素材的 scene / shot。
- 项目素材标签页的「关联状态」列显示每个素材是否已关联日志。
- 切换到拍摄日志界面时会自动刷新项目列表。
- 删除日志使用事务级联清理，保证数据一致性。

---

## 模块 7: 素材检索

### 界面布局

- **搜索条件组**：
  - 第一行：项目下拉框 + 关键词输入框
  - 第二行：场景 / 镜头 / 类型下拉框
  - 第三行：拍摄日志 + 评级 + 标签关键字（带自动补全）
  - 第四行：日期范围（「限定日期范围」复选框 + 起止日期）
- **操作按钮**：🔍 搜索 / 重置 / 📤 导出 CSV / 🔁 跨项目查重。
- **结果计数标签**：显示匹配总数与当前页码。
- **分页控制条**：结果分页展示（默认每页 500 条），支持「◀ 上一页 / 下一页 ▶」。
- **过期提示条**：数据变更后提示结果可能过期，点击可重新搜索。
- **结果表格**：9 列（文件名 / 类型 / 大小 / 场景 / 镜头 / 关联日志 / 评级 / 校验和 / 导入时间），双击行可在文件管理器中打开所在目录。

### 操作流程

1. 选择项目（可选「全部项目」）。
2. 输入关键词（按文件名模糊匹配，走 FTS5 全文索引，亦覆盖场景 / 镜头 / 备注 / 标签）。
3. 可选填写场景号、镜头号、选择文件类型。
4. 可选选择评级档位（全部评级 / ★可用及以上 / ★★备选及以上 / ★★★优选）。
5. 可选选择拍摄日志（筛选关联了某条日志的素材）。
6. 可选输入标签关键字，按素材自定义标签筛选。
7. 可选勾选「限定日期范围」并设置起止日期。
8. 点击「搜索」按钮，结果表格显示匹配的素材，超过一页时可用分页条翻页。
9. 点击「重置」清空所有条件。

### 导出 CSV 与跨项目查重

- **📤 导出 CSV**：把当前搜索条件匹配的全部素材（不分页）导出为 CSV 素材清单（utf-8-sig 编码，Excel 可直接打开）。
- **🔁 跨项目查重**：按校验和聚合所有项目中的重复入库素材，弹出对话框列出重复分组，双击可定位文件所在目录，用于人工清理重复记录。

### 评级筛选

评级下拉框提供 4 个选项：

| 选项 | 含义 |
|------|------|
| 全部评级 | 不按评级筛选 |
| ★ 可用及以上 | 筛选 rating ≥ 1 的素材 |
| ★★ 备选及以上 | 筛选 rating ≥ 2 的素材 |
| ★★★ 优选 | 筛选 rating = 3 的素材 |

### 注意事项

- 关键词按文件名模糊匹配（FTS5 索引，未启用时自动回退 LIKE 查询）。
- 结果分页展示（每页 500 条），避免大列表卡顿；CSV 导出不受分页限制。
- 切换项目时拍摄日志下拉框会自动刷新。
- 数据变更后（导入/备份/日志修改），顶部会显示「结果可能过期」提示条，点击可重新搜索。

## 模块 8: 素材信息

### 界面布局

- **顶部**：工作区/项目选择器 + 「🔄 一键重新读取全部 EXIF」+ 「📤 导出素材清单 CSV」+ 「🧹 清理丢失素材」按钮
- **左侧**：素材列表表格（文件名 / 类型 / 大小 / EXIF / 状态），支持多选（Ctrl / Shift）
- **底部**：批量操作栏（批量评级 + 🗑 删除选中），并显示已选计数与进度
- **右侧**：素材详情面板，分 7 大信息分组：
  - **基本信息**：文件名、路径、大小、类型、导入时间、拍摄时间
  - **拍摄参数**：摄影机品牌、型号、镜头、焦距、ISO、光圈、快门
  - **场景信息**：场景、镜头、镜次、关联日志
  - **媒体规格**：宽度、高度、时长、视频编码
  - **备份与校验**：校验算法、校验值、备份位置
  - **镜次评级**：4 个评级按钮（未评级 / ★ 可用 / ★★ 备选 / ★★★ 优选）
  - **标签与备注**：标签输入框（逗号分隔，支持自动补全）+ 备注输入框 + 「💾 保存标签与备注」按钮

### 操作流程

1. 在顶部选择工作区与项目。
2. 左侧素材列表显示该项目下所有素材。
3. 点击素材，右侧详情面板显示完整 EXIF 与元数据信息。
4. **设置评级**：在「镜次评级」行点击对应评级按钮，立即保存到数据库并广播刷新。
5. **重新读取单个素材 EXIF**：选中素材后点击「读取元数据」按钮。
6. **批量重新读取 EXIF**：点击顶部「🔄 一键重新读取全部 EXIF」按钮，后台线程逐个读取，进度条显示进度。
7. **编辑标签与备注**：在「标签与备注」分组输入标签（多个用逗号分隔）与备注，点击「保存标签与备注」立即生效，可在「素材检索」按标签筛选。
8. **批量评级 / 删除**：在素材列表多选素材，底部批量栏一键设置评级（★ 可用 / ★★ 备选 / ★★★ 优选 / 未评级），或「删除选中」从项目移除记录（不删除磁盘文件）。
9. **导出素材清单**：点击「📤 导出素材清单 CSV」把当前项目的全部素材元数据（含标签、备注、评级）导出为 CSV（Excel 可直接打开）。
10. **检查文件状态**：素材列表加载后在后台检查文件路径，状态列显示「✓ 正常」或「⚠ 文件已丢失」；检查期间可以继续浏览界面。
11. **清理丢失素材**：点击「🧹 清理丢失素材」后，系统会在后台再次检查文件状态；确认后仅删除数据库中的失效素材记录，不会删除磁盘文件。
12. **重新链接移动后的素材**：文件丢失时点击「重新链接丢失素材」，选择素材的新根目录。系统先显示预览：优先按旧根目录下的相对路径匹配，再按文件名与大小匹配；有多个候选文件的条目必须手动选择，不会自动覆盖。

### 镜次评级

评级共 4 档：

| 评级值 | 标签 | 说明 |
|--------|------|------|
| 0 | 未评级 | 默认值 |
| 1 | ★ 可用 | 可直接使用 |
| 2 | ★★ 备选 | 备选素材 |
| 3 | ★★★ 优选 | 推荐使用 |

- 设置评级后立即保存，无需额外点击「保存」按钮
- 评级设置后会广播 `assets_changed` 事件，其他视图（检索/看板）自动刷新
- 评级可在「素材检索」视图中作为筛选条件

### 注意事项

- 评级设置后立即生效，其他视图会自动刷新
- 批量读取 EXIF 时，视频文件读取视频元数据（需 MediaInfo），图片/RAW 读取 EXIF
- 单个文件读取异常不会中断批量流程，会在进度中显示错误信息
- 未导入元数据的素材显示为占位符「—」
- 文件存在性检查在后台执行，网络盘或大型项目不会因状态扫描长时间阻塞界面；扫描完成前状态列显示「检查中…」
- 「清理丢失素材」只清理数据库记录，不会删除源文件；删除前会二次确认，且未知或重复记录不会计入删除数量
- 素材信息列表按页加载，默认每页 500 条；使用列表下方的「上一页 / 下一页」浏览其余素材。CSV 导出仍会导出当前项目的全部素材。
- CSV 导出在后台流式写入，可在进度窗口点击「取消」。取消后已写入的输出文件不应作为完整交付物使用。

---

## 模块 9: 报告生成

### 界面布局

- **报告配置组**：工作区/项目选择器、报告类型下拉框、输出路径选择。
- **操作按钮**：生成报告按钮与取消按钮。
- **状态标签** 与 **生成日志**。

### 操作流程

1. 从下拉框选择项目。
2. 选择报告类型：
   - **数据备份报告**：包含项目信息、备份任务详情、目标状态。
   - **素材统计报告**：包含项目信息、素材按场景统计、镜次评级分布。
3. 选择输出路径（默认 `~/Documents/DIT_Reports/`，文件名带时间戳）。
4. 点击「生成报告」，后台迭代素材并生成 PDF；界面仍可响应。
5. 如需中止，点击「取消」，系统会在当前素材统计批次结束后停止。
6. 完成后弹窗显示报告路径。

### 素材统计报告内容

素材统计报告包含以下部分：
- **项目信息**：项目名称、描述、创建时间
- **统计概览**：素材总数、已评级素材数、总大小
- **按场景统计**：每个场景的素材数量与占比
- **按评级统计**：每个评级档位的素材数量与占比（「★★★ 优选」行高亮显示）

### 注意事项

- PDF 报告会按平台自动查找中文字体，但必须至少存在一个可用字体。
- 报告文件名格式包含时间戳，不会覆盖已有报告。
- macOS: PingFang 字体；Windows: 微软雅黑；Linux: 文泉驿 / Noto。
- 若系统没有上述字体，报告生成可能失败；请先安装中文字体后再生成 PDF。

---

## 设置与维护

在「设置」菜单（`Ctrl+,`）中打开设置对话框，包含以下分组：

### ♻️ 回收站

通过「设置 → 回收站…」打开。这里显示保留期内通过删除操作移入回收站的**数据库记录**：

1. 选中一条素材或项目记录。
2. 点击「恢复选中项」恢复单条记录，或点击「还原全部记录」批量恢复。恢复成功后，记录会从回收站移除，并刷新项目、素材和统计视图。
3. 点击「清空回收站」可永久删除全部回收站快照；操作会二次确认，且不会删除磁盘上的源媒体文件。
4. 同 ID 的记录已存在、素材所属项目不存在等冲突时，系统不会覆盖现有数据，并会提示失败原因。

> 回收站不会删除或恢复磁盘上的源媒体文件，只管理 SQLite 中的项目、素材、标签、日志关联和备份位置等元数据。到期记录会在刷新回收站时自动清理。

### 🧾 后台任务中心

通过「设置 → 任务中心…」打开。任务中心记录后台导入、备份、校验等任务的创建时间、所属项目、状态、完成时间和错误摘要。

1. 可按项目或任务状态筛选，并选择任务查看参数、恢复上下文、输出和错误详情。
2. 带备份作业 ID 的失败、取消或可恢复备份任务会显示「重试失败备份」入口；重试优先只处理原作业快照中的失败文件，快照缺失时才回退扫描源目录。
3. 任务中心不会删除原始文件；重试仍遵循备份服务的校验和验证与断点续传规则。

校验和结果会同时保存在有容量上限的进程内 LRU 缓存和 SQLite 持久化缓存中，持久化缓存也会按访问时间淘汰旧条目。应用重启后，文件路径、大小、修改时间或算法未变化时可直接复用结果；文件发生变化会自动重新计算。

同一素材的并发缩略图请求会合并为一次后台生成，其他请求复用同一个结果，避免重复读取和解码。

### 📂 数据存储位置

- 可分别查看 / 更改 / 打开数据库目录、报告输出目录、日志目录与缩略图缓存目录。
- **数据库目录更改**：自动关闭连接、把数据库文件（含 WAL/SHM）移动到新位置并持久化，重启应用后生效。
- 报告 / 日志 / 缩略图目录更改立即生效并持久化；历史文件保留在原目录，可手动迁移。

### 🧹 清理临时文件

- **🗑 删除日志文件**：一键删除日志文件（含轮转文件），删除前显示数量与占用空间。
- **🗑 清理缩略图缓存**：删除缩略图缓存，下次查看时自动重新生成。

### 📁 最近路径

- 显示最近使用路径记录总数，可一键「清空最近路径记录」。

### 📦 备份默认选项

- 「拷贝后默认验证完整性」：设置备份视图中「拷贝后验证」复选框的默认状态（可在备份视图临时调整）。

### 💾 存储卡与相机卡自动化

- 「检测到存储卡时自动跳转到导入视图」：插入含 `DCIM`/`AVCHD`/`PRIVATE` 等相机目录或至少 3 个媒体文件的存储卡时，自动切换到「媒体导入」并预填源目录、开始扫描。
- 「检测到相机卡后自动执行配置」：勾选后，可按下方配置的「自动关联项目」与「自动备份方案」，在插卡后自动完成「自动导入素材」和/或「自动执行备份方案」。
- 未配置完整（如未选项目或备份方案）时，插卡仅提示，不会启动任务。

---

## FAQ 常见问题

**Q1: 数据库文件存在哪里？**
A: 开发模式下位于项目目录 `data/dit_workstation.db`；打包后 macOS 位于 `~/Library/Application Support/DITWorkstation/`，Windows 位于 `%APPDATA%\DITWorkstation\`，Linux 位于 `~/.local/share/DITWorkstation/`。若默认目录不可写，会回退到 `~/.ditworkstation/`，必要时使用系统临时目录。

**Q2: 导入时重复文件会怎样？**
A: 系统按项目内文件路径查重，已导入的文件会被自动跳过并计入「跳过」数量。

**Q3: 导入 / 备份过程中可以取消吗？**
A: 可以，点击「取消」按钮即可中断，已处理的部分不受影响。

**补充：CSV 导出、报告和归档可以取消吗？**
A: 可以。CSV 导出使用进度窗口的「取消」按钮；报告使用「取消」按钮；归档在项目概览的任务进度区取消。取消会在当前批次或当前文件完成后生效。归档采用临时 ZIP 写入，取消不会覆盖已有目标归档；取消后的 CSV/PDF 输出不应作为完整交付物使用。

**Q4: 复制模式下重名文件怎么处理？**
A: 自动在文件名后加 `_N` 后缀（如 `file_1.cr2`、`file_2.cr2`）。

**Q5: 报告中中文显示乱码或无法生成怎么办？**
A: 报告会查找系统中文字体（macOS: PingFang，Windows: 微软雅黑，Linux: 文泉驿 / Noto）。请先安装可用的中文字体并重新生成报告；没有候选字体时，当前版本可能无法生成 PDF。

**Q6: 删除项目会删除实际文件吗？**
A: 不会。删除项目仅删除数据库记录（项目、日志、素材记录），不会删除磁盘上的实际文件。

**Q7: 校验和有什么用？**
A: 校验和是文件的唯一指纹，用于查重和完整性验证。备份后验证可确保拷贝无误。

**Q8: 拍摄日志和素材如何关联？**
A: 有两种方式：1) 创建日志时选择关联素材；2) 在「项目素材」标签页多选素材后创建日志。关联后素材的 scene / shot 会同步日志信息。

**Q9: 工作区和项目是什么关系？**
A: 工作区是项目的父级容器，对应一个物理目录，用于组织一组相关项目。一个工作区下可创建多个项目。默认工作区不可删除，删除非默认工作区时其下项目自动归入默认工作区。

**Q10: 镜次评级怎么使用？**
A: 在「素材信息」视图选中素材后，点击评级按钮（未评级 / ★ 可用 / ★★ 备选 / ★★★ 优选）即可设置。在「素材检索」视图可按评级筛选。「报告生成」的素材统计报告含评级分布统计。

**Q11: 首启向导弹出条件是什么？**
A: 仅在数据库中无任何项目时自动弹出。若取消向导，后续可在各功能模块左侧面板手动创建工作区和项目。

**Q12: 复制到工作区时目录如何确定？**
A: 在「媒体导入」视图勾选「复制到工作区」后，目录自动取自当前选中工作区的物理路径，无需手动选择。若工作区未设置物理目录，则需手动选择。

**Q13: 重命名后数据库记录会更新吗？**
A: 会。执行重命名后，数据库中素材的 `file_path` 会自动同步为新路径，无需重新导入。

**Q14: RAW 提取后能自动入库吗？**
A: 能。在 RAW 提取视图顶部选择要关联的项目后，提取出的 RAW 文件会自动入库到该项目，并继承原 JPG 的日志关联（log_id）。

**Q15: 视频元数据读取失败怎么办？**
A: macOS/Windows 的正式发布包已携带 MediaInfo。只有从源码构建时才需要在 macOS 运行 `brew install mediainfo`，或在 Windows 安装 MediaInfo/设置 `MEDIAINFO_DLL`，随后重新构建应用。

**Q16: 搜索结果超过 500 条怎么看全部？**
A: 检索结果按页展示（默认每页 500 条），使用「◀ 上一页 / 下一页 ▶」翻页；「📤 导出 CSV」会把全部匹配结果（不分页）一次性导出。

**Q17: 素材标签怎么设置和使用？**
A: 在「素材信息」视图的「标签与备注」分组输入标签（多个用逗号分隔，如 `日戏,主镜头`），保存后立即生效；「素材检索」视图可按标签关键字筛选（带自动补全）。标签与备注会随 CSV 导出与项目归档一起保存。

**Q18: 归档 / 恢复会损坏我的素材吗？**
A: 不会。恢复采用「临时目录写入 → 校验全部通过后再移入目标」策略，校验失败的归档不会留下半成品；同时内置成员路径、解压大小与符号链接安全检查，拒绝恶意归档。归档包可先在任何 zip 工具中打开检查。

**Q19: 相机卡自动化怎么配置？**
A: 在「设置 → 存储卡」勾选「检测到相机卡后自动执行配置」，再选择「自动关联项目」与「自动备份方案」（备份方案需先在「数据备份」视图保存），并按需勾选「自动导入素材」和/或「自动执行备份方案」。

**Q20: 备份中断了怎么办？**
A: 不需要重新备份。在「数据备份」视图下方的「备份历史」选中该作业，点击「↻ 重试失败文件」，系统只补拷缺失/失败文件，目标中已存在且校验一致的文件自动跳过（断点续传）。

**Q21: 备份方案有什么作用？**
A: 备份方案保存一组备份目标、校验算法与验证选项，可下拉复用，避免每次手工重复配置；同时作为「相机卡自动化」自动备份的依据。

**Q22: 素材信息中的「文件已丢失」是什么意思？**
A: 应用在后台检查素材数据库中记录的路径。如果文件已被移动、删除、存储盘未连接或路径为空，会标记为「⚠ 文件已丢失」。状态检查不会修改文件。

**Q23: 「清理丢失素材」会删除我的照片或视频吗？**
A: 不会。该功能只删除数据库中的失效素材记录，不操作磁盘文件；执行前会再次检查并弹出确认框。正常存在的素材不会被清理。

**Q24: 删除的项目或素材如何恢复？**
A: 打开「设置 → 回收站…」，选中记录后点击「恢复选中项」。回收站仅恢复数据库元数据，不会从磁盘还原已被手动删除的源文件；若同 ID 已被重新使用或所属项目不存在，系统会拒绝覆盖以保护现有数据。

**Q25: 如何查看当前版本？**
A: 查看窗口标题、底部状态栏，或打开「帮助 → 关于 DIT 工作站」。版本格式为 `alpha.YYYYMMDD`，具体日期由启动或打包当天决定，例如 `alpha.20260821`。
